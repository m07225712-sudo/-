name: money_tracker
description: Money Tracker App for Test
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  auto_route: ^11.2.0
  drift: ^2.14.0
  drift_flutter: ^0.3.1
  path_provider: ^2.1.1
  path: ^1.8.3
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.7
  auto_route_generator: ^10.6.0
  drift_dev: ^2.14.1
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
